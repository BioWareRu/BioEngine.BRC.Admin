import { Component } from '@angular/core';
import { AbstractFormInput } from './abstract-form-input';

@Component({
    selector: 'checkbox-input',
    templateUrl: './CheckboxInputComponent.html'
})
export class CheckboxInputComponent extends AbstractFormInput {
}
